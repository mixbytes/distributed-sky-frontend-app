import Events from 'consts/Events';

const ZoneadditionFormItems = {
    consts_BC: {
        BITMAP_CELL_LENGTH: 1,
        BITMAP_CELL_WIDTH: 1,
        CELL_SIZE_DEGREE: 2,
    },
};

export default ZoneadditionFormItems;
