import Events from 'consts/Events';

const ExtractFromStorageFormItems = {
    accountAddress: {
        inputID: 'accountAddress',
        inputName: 'Account address: ',
        inputType: 'text',
        event: Events.InputAddress,
    },
};

export default ExtractFromStorageFormItems;
